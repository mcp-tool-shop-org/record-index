# E01 — executor kickoff

The dispatches for E01, each one a session handoff with its own header.

## Session handoff 1 (2026-01-04, executor) — the first dispatch

The first executor seat ran the control sequence and reported evidence. Commits
`a1b2c3d` and `deadbee` carry the run.

## Session handoff 2 (2026-01-08, executor) — SUPERSEDED by the closing dispatch

Nothing to carry forward. This handoff is marked superseded in its own header, which
is the only place the flag is read from.
