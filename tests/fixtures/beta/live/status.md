# beta, as it stands

A live status document. It sits under the declared current-state directory, so a count
here that disagrees with the record is staleness rather than history.

## The count

A01 carries 7 decisions, which is wrong. The sweep must reach STALE through the
directory branch rather than through the file list, which is the branch alpha's
OVERVIEW.md exercises.
