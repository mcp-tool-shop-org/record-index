# A01 — decision

This document carries BOTH declared header forms, which is the case a single hard-coded
ruling-header pattern silently reads as zero rows.

## 1. RULING — the first route is UPHELD

2026-03-01. The Chief looked at the whole route and it holds end to end, so it is the
one the next arc builds on. The clip outputs/a01/route.mp4 is the evidence.

## Decision 2 — the second route is OVERTURNED

2026-03-02. The second route was chosen for a reason that did not survive contact with
the measurement, so it leaves the record rather than sitting in it unmarked.

## 3. What this file is not

This heading is numbered and announces nothing, so no declared header form reads it.
The ruling-header vocabulary counts it as unrecognised and names it, which is exactly
how a second repo's header form was found instead of being left silently at zero rows.
