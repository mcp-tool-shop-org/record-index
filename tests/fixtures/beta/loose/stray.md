# A stray document

This file sits under no declared corpus and under no declared root prefix, so the
declaration audit must report it as undeclared-but-present.

Reporting only what a declaration named would make the audit the hardcoded-list defect
wearing a different hat: a list can always say what it contains and never what it
missed. Both directions, or the audit is not one.
