# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# derived indexes and their determinism temps — never committed from here;
# a record repo commits its own db + certificate pair, this repo builds neither
*.db
*.db.det_a.*
*.db.det_b.*
*.cert.json

# test / tooling
.pytest_cache/
.coverage
htmlcov/

# Site (Astro/Starlight under site/). The lockfile IS committed — CI runs
# `npm ci`, so the build that runs there is the build verified on the rig.
site/node_modules/
site/dist/
site/.astro/
